import {CustomizerDirective} from './customizer.directive';

describe('CustomizerDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomizerDirective();
    expect(directive).toBeTruthy();
  });
});
